{
  "name": "moneysplit",
  "version": "1.0.0",
  "description": "MoneySplit - Commission splitting and tax calculation platform",
  "scripts": {
    "postinstall": "cd frontend && npm install --legacy-peer-deps && npm run build",
    "build": "cd frontend && npm run build"
  },
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  }
}
